next-version: 1.0.5
tag-prefix: '[vV]'
mode: ContinuousDeployment
branches:
  master:
    regex: ^master$
  release:
    regex: ^release$
  develop:
    regex: ^develop$|^dev$
    tag: beta
  pull-request:
    tag: beta
ignore:
  sha: []